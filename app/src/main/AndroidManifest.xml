<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Receive permission-gated BgEstimate broadcasts from xDrip+. xDrip+
         sends with sendBroadcast(intent, RECEIVER_PERMISSION) by default, so
         the receiver must hold this custom permission or broadcasts are
         silently dropped by the OS before delivery. -->
    <uses-permission android:name="com.eveningoutpost.dexdrip.permissions.RECEIVE_BG_ESTIMATE" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.GlucoseSync"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config">

        <meta-data
            android:name="com.google.android.wearable.capabilities"
            android:resource="@xml/wearable_capabilities" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.GlucosePollingService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="CGM glucose monitoring and alerting" />
        </service>

        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.PollingAlarmReceiver"
            android:exported="false" />

        <!-- Manifest-declared receiver for xDrip+/Diabox glucose broadcasts.
             This is the durability backstop for the runtime receiver in
             XdripBroadcastProvider: it captures broadcasts even while the
             GlucoseSync process is otherwise idle (Doze, post-kill, etc.),
             persists the reading so it survives process death, and ensures
             the polling service is running.

             android:exported="true" (required on API 31+) because xDrip+ is a
             different app. The action (com.eveningoutpost.dexdrip.BgEstimate)
             is a custom action, so the Android 8.0+ implicit-broadcast ban
             does NOT apply — a manifest receiver can legitimately receive it.

             No android:permission is set here, matching the runtime receiver
             (RECEIVER_EXPORTED with no sender-side check). xDrip+'s send is
             permission-gated on the RECEIVER side via sendBroadcast(intent,
             RECEIVER_PERMISSION); our <uses-permission> for
             RECEIVE_BG_ESTIMATE (declared above) is what lets that delivery
             through. Adding a sender-side check here would be stricter than
             the runtime receiver and could drop Diabox-style BgReading
             broadcasts (which don't hold xDrip+'s custom permission). -->
        <receiver
            android:name=".receiver.XdripBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.eveningoutpost.dexdrip.BgEstimate" />
                <action android:name="com.eveningoutpost.dexdrip.BgReading" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
